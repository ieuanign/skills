# Fixture — a pull request with no unresolved comments

A throwaway file. Its pull request carries no review comments at all, which is the state an
unattended `/pr-comments auto` run ends at Step 2 on. It is deleted with that pull request.
