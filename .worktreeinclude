# Gitignored files that worktrees need, in gitignore syntax.
# Nothing to copy: .claude/ is copied by provisioning itself, and .DS_Store is OS noise.

# MUST stay last — gitignore matching is last-match-wins, so only the final
# position reliably stops copy mechanisms cloning .scratch (worktrees included).
!.scratch/**
