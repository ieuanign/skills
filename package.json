{
  "name": "ieuanign-skills",
  "version": "0.16.0",
  "private": true,
  "description": "Ieuan's add-on agent skills for real engineering — a dev-loop issue-to-PR pipeline over a custom agent roster, plus coding-standards setup. Layers on mattpocock/skills.",
  "repository": {
    "type": "git",
    "url": "https://github.com/ieuanign/skills"
  },
  "license": "MIT",
  "scripts": {
    "changeset": "changeset",
    "check": "bash scripts/check.sh",
    "version": "changeset version && bash scripts/sync-plugin-version.sh"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.30.0"
  },
  "overridesComment": "human-id 4.2.0 shipped `type: module` in a MINOR bump, so every CJS consumer broke. @changesets/write@0.4.0 requires it and floats into it via ^4.1.1, which made every `changeset` command die with ERR_REQUIRE_ESM. 4.1.3 is the last CJS release. Drop the override once @changesets/write imports it dynamically.",
  "overrides": {
    "human-id": "4.1.3"
  },
  "packageManager": "npm@10.9.4"
}
