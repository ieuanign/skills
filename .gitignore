node_modules/
.DS_Store
.scratch/
*.log

# Lane worktrees, which /dev-loop nests inside the main checkout.
.claude/worktrees/

# Dogfooding links written by scripts/link-skills.sh — symlinks back into this
# repo's own skills/ and agents/, recreated on demand and never committed.
.claude/skills/
.claude/agents/
