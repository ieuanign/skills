# Coding Standards — Review Rubric

Distilled from this repo's CLAUDE.md files. CLAUDE.md remains the binding source; this file is a derived rubric organized for review-time scanning. Regenerate by hand (`/setup-ieuanign-skills`) if CLAUDE.md changes materially — nothing re-syncs it automatically.

## <Area — e.g. "General", "Backend", "Frontend">

### Hard rules

- <rule, closely paraphrased or quoted> — source: `<file>`, "<section>"

### Smell-baseline overrides

- **<Smell name>** — <the repo pattern that would otherwise trip it> — <why it's not a violation here>

<!-- Repeat the two subsections above once per area. A single-context repo gets one "## General" section; a multi-context repo gets one section per context, plus "## General" for whatever the root CLAUDE.md states repo-wide. -->
